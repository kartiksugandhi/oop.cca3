#include <iostream>
#include <string>
using namespace std;

int main() {
    int a, b;
    cin >> a;
    cin >> b;
    
    // array mapping for numbers 1 through 9
    string names[10] = { "", "one", "two", "three", "four",
                         "five", "six", "seven", "eight", "nine" };
    
    for (int i = a; i <= b; i++) {
        if (i >= 1 && i <= 9) {
            cout << names[i] << endl;
        } else {
            if (i % 2 == 0) {
                cout << "even" << endl;
            } else {
                cout << "odd" << endl;
            }
        }
    }
    
    return 0;
}
